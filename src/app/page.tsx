import React from "react";
import Header from "./components/Header";

const Home = () => {
  return (
    <div>
      {/* adding the header */}
      <Header />
    </div>
  );
};

export default Home;
